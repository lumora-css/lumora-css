{
  "name": "lumora",
  "version": "1.0.0",
  "description": "Premium fast UI design and components framework",
  "main": "index.html",
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun x concurrently \"bun x nodemon --watch src -e scss,js --exec \\\"bun run build.js\\\"\" \"bun run docs-server.js\"",
    "docs:dev": "bun run docs-server.js",
    "docs:build": "echo 'No build step required. Server dynamically renders markdown.' && exit 0"
  },
  "dependencies": {
    "clsx": "^2.0.0",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "sass": "^1.101.0",
    "typescript": "~6.0.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  },
  "keywords": [
    "ui",
    "css",
    "framework"
  ],
  "author": "",
  "license": "ISC"
}
