---
description: Turn any folder of files into a navigable knowledge graph and Follow Project Rules
---

# Workflow: graphify

Follow the graphify skill installed at ~/.gemini/config/skills/graphify/SKILL.md to run the full pipeline.

If no path argument is given, use `.` (current directory).

# Follow Rules for this project

- When any new feature added or get updated, always update index.html with example.
- Always create or update docs/feautre.md with example classes code how to use, if any missing just create new if already then check if need to update. also each example says check this xyz.md for usage code
- Always try to use sass function better to avoid automated logic to create manully
- Always use same classes name as Bootstrap 5
- After doing chages in scss always run build command to test if all success